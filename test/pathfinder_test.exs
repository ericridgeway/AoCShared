defmodule AoCSharedTest.Pathfinder do
  # use ExUnit.Case
  # alias AoCShared.Ascii
  # alias AoCShared.Pathfinder
  # alias AoCShared.Coord

  # test "All neighbors" do
  #   board = """
  #   ...
  #   ...
  #   ...
  #   """
  #   |> Ascii.to_map
  #   source = {1,1}
  #   neighbors_func = &Coord.all_adjacent/1

  #   parents = Pathfinder.parents(board, source, neighbors_func)
  #   assert Pathfinder.parent_path(parents, {3,3} == [{1,1}, {2,2}, {3,3}])
  # end
end
